# Lab 00 Solution: Capability Check

## 🎓 Фундаментальное объяснение

### Почему модели "не умеют" Tools?
LLM (Large Language Model) — это вероятностный генератор текста. Она не "знает" про функции.
Механизм **Function Calling** — это результат специальной тренировки (Fine-Tuning).
Разработчики модели (OpenAI, Mistral, Meta) добавляют в обучающую выборку тысячи примеров вида:
`User: "Check weather" -> Assistant: <special_token>call_tool{"name": "weather"}<end_token>`

Если вы скачали "голую" Llama 3 (Base model), она не видела этих примеров. Она просто продолжит диалог.
Если вы скачали Llama 3 Instruct, она обучена отвечать на вопросы, но не обязательно обучена формату инструментов OpenAI.

### Зачем нужна температура 0?
В коде вы видите `Temperature: 0`.
Температура регулирует "случайность" выбора следующего токена.
*   **High Temp (0.8+):** Модель выбирает менее вероятные слова. Хорошо для стихов.
*   **Low Temp (0):** Модель всегда выбирает самое вероятное слово (ArgMax).
Для агентов, которые должны выдавать строгий JSON или вызов функции, нужна максимальная детерминированность. Любая "творческая" ошибка в JSON сломает парсер.

### Как работает этот тест?
Это **Unit-тестирование** для нейросети.
В Software Engineering мы пишем тесты для кода.
В AI Engineering мы пишем **Evals** (Evaluations) для моделей.
Этот скрипт — примитивный Eval. В промышленных системах (LangSmith, PromptFoo) таких тестов сотни.

